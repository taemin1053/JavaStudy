package codingTest.DFSBFS;

public class DFS {
    /*
---

# DFS (깊이 우선 탐색, Depth-First Search)

## 1. 정의 및 핵심 개념

DFS는 그래프나 트리 구조에서 **한 루트로 탐색을 시작하여, 특정 분기(Branch)를 완벽하게 탐색한 후 다음 분기로 넘어가는 탐색 방법**입니다.

* **직관적 이해:** 미로 찾기를 할 때 한쪽 벽만 계속 짚고 끝까지(막다른 길) 갔다가, 길이 막히면 가장 최근의 갈림길로 되돌아와서 다른 길을 찾는 방식입니다.
* **목적:** "모든 노드를 빠짐없이 방문해야 할 때" 또는 "경로의 특징을 저장하며 탐색해야 할 때" 사용합니다.

---

## 2. 동작 메커니즘 (Mechanism)

DFS는 **후입선출(LIFO, Last-In-First-Out)** 원칙을 따릅니다. 이를 구현하기 위해 자료구조 스택(Stack)을 사용하며, 실무와 코딩 테스트에서는 명시적인 `Stack` 클래스보다 재귀 함수(Recursion)가 만드는 호출 스택(Call Stack)을 활용하는 것이 표준입니다.

**[동작 순서]**

1. 시작 노드를 스택에 넣고 방문 처리(`visited = true`)를 합니다.
2. 스택 최상단 노드에 방문하지 않은 인접 노드가 있다면, 그 인접 노드를 스택에 넣고 방문 처리합니다. (깊이 파고들기)
3. 방문하지 않은 인접 노드가 없으면(막다른 길), 스택에서 최상단 노드를 꺼냅니다. (백트래킹, 복귀)
4. 스택이 텅 빌 때까지 2번과 3번을 반복합니다.
[탐색 순서 예시: 트리 구조]
        (1)
       /   \
     (2)   (5)
     / \
   (3) (4)

탐색 경로: 1 -> 2 -> 3 -> (복귀) -> 4 -> (복귀) -> (복귀) -> 5

1.전진(Dive): 갈 수 있는 자식 노드가 있으면 스택 프레임에 현재 함수의 실행 위치를 쌓아두고 자식 노드를 호출합니다.
2.기저 조건 도달(Base Case): 더 이상 갈 수 없거나 목표 길이에 도달하면 return으로 함수를 종료합니다.
3.복귀(Backtrack): 스택의 맨 위 함수가 소멸하면서, 직전에 호출했던 부모 함수의 for 루프 다음 줄로 제어권이 되돌아옵니다.
---

## 3. 시간 복잡도와 공간 복잡도

탐색해야 하는 정점(Vertex)의 수를 $V$, 간선(Edge)의 수를 $E$라고 할 때, 그래프를 어떻게 구현했느냐에 따라 시간 복잡도가 달라집니다.

* **시간 복잡도:**
* **인접 리스트 (List의 배열):** O(V + E)
* 각 정점마다 연결된 간선만 확인하므로 가장 효율적입니다. 코딩 테스트 그래프 문제의 기본.


* **인접 행렬 (2차원 배열):** $O(V^2)$
* 연결 여부를 확인하기 위해 정점 1개당 $V$번의 루프를 다 돌아야 합니다. V \le 1000 이하의 조밀한 그래프(Dense Graph)에서만 사용합니다.




* **공간 복잡도:**
* 그래프의 최대 깊이를 $h$라고 할 때, 재귀 호출 스택이 깊어지는 최대치인 $O(h)$의 메모리를 사용합니다. BFS($O(V)$)보다 보통 메모리 소모가 적습니다.



---

## 4. DFS 구현 시 반드시 지켜야 할 3요소

DFS를 짤 때 헷갈리는 이유는 상태 관리가 섞이기 때문입니다. 아래 세 가지 경계를 명확히 나누어야 합니다.

1. **기저 조건 (Base Condition / Exit):**
* **역할:** 무한 루프(Stack Overflow) 방지. 재귀를 "언제 멈추고 돌아갈 것인가"를 결정.
* **위치:** 재귀 함수 **가장 첫 줄**에 작성 (`if (depth == limit) return;`)


2. **독립 상태 (매개변수, Parameter):**
* **역할:** 각 갈림길(탐색 경로)마다 **서로 간섭 없이 독립적으로 가져가야 하는 값**.
* **예시:** 현재 깊이(`depth`), 현재까지의 누적 비용(`sum`), 남은 체력(`k`).


3. **공유 상태 (클래스 필드, Field):**
* **역할:** 모든 재귀 경로가 공통으로 바라보고 갱신하거나 읽기만 하는 값.
* **예시:** 맵 원본 배열(`map`), 최종 정답 카운트(`maxCount`), 방문 체크 배열(`visited`).



---

## 5. 핵심 패턴 및 구현 예제 (Java)

코딩 테스트의 DFS는 크게 2가지 패턴으로 귀결됩니다. **방문(visited) 상태를 복구하느냐, 마느냐**의 차이입니다.

### 패턴 A: 단순 영역 탐색 (방문 복구 X)

* **용도:** 섬의 개수 구하기, 바이러스 전파, 연결된 송전탑 개수 세기.
* **특징:** 이미 밟은 땅은 다른 경로에서 다시 밟을 이유가 없으므로 `visited = false`로 되돌리지 않습니다.

```java
class AreaExplorer {
    private final boolean[][] map;
    private final boolean[] visited; // 공유 상태

    public AreaExplorer(boolean[][] map, int n) {
        this.map = map;
        this.visited = new boolean[n + 1];
    }

    public int dfs(int node) {
        visited[node] = true; // 1. 방문 처리 (다시 풀지 않음)
        int count = 1;

        for (int next = 1; next < visited.length; next++) {
            // 2. 연결되어 있고 방문하지 않았다면 계속 전진
            if (map[node][next] && !visited[next]) {
                count += dfs(next);
            }
        }
        return count; // 연결된 총 노드 개수 반환
    }
}

```

### 패턴 B: 순열/조합 백트래킹 (방문 복구 O)

* **용도:** N-Queen, 피로도 던전 순열, 가능한 모든 암호 생성.
* **특징:** A경로에서 방문했던 노드를, B경로로 처음부터 다시 탐색할 때는 재사용해야 하므로 재귀가 끝난 후 상태를 반드시 **원상복구(Backtracking)** 해야 합니다.

```java
class PathFinder {
    private int maxScore = 0; // 공유 상태

    public void dfs(int depth, int currentSum, boolean[] visited, int[] nodes) {
        // 1. 기저 조건: 바닥을 찍었을 때 결과 갱신 후 탈출
        if (depth == nodes.length) {
            maxScore = Math.max(maxScore, currentSum);
            return;
        }

        for (int i = 0; i < nodes.length; i++) {
            if (!visited[i]) {
                visited[i] = true; // ① 상태 전이 (문 잠금)

                // ② 재귀 파고들기 (독립 상태인 depth와 sum을 넘김)
                dfs(depth + 1, currentSum + nodes[i], visited, nodes);

                visited[i] = false; // ③ 상태 복구 (문 다시 열기 - 핵심!)
            }
        }
    }
}

```

---

## 6. 장단점 및 알고리즘 선택 기준

### 장점 (Pros)

1. **메모리 효율:** 현재 탐색 중인 경로만 스택에 저장하므로 너비 우선 탐색(BFS)에 비해 메모리 공간을 덜 차지합니다.
2. **구현의 간결함:** 재귀를 사용하면 코드가 매우 짧고 직관적입니다.
3. **가지치기(Pruning) 최적화:** 탐색 도중 "이 경로는 더 이상 가봤자 정답이 될 수 없다"고 판단되면 즉시 `return`하여 불필요한 연산을 대폭 줄일 수 있습니다(백트래킹의 핵심).

### 단점 (Cons)

1. **최단 경로 보장 불가:** 해를 발견하면 즉시 반환하거나 갱신하는데, 이 해가 '최단 경로'라는 보장이 없습니다. 최단 경로를 찾으려면 결국 모든 경로를 다 탐색해야 합니다.
2. **스택 오버플로우 위험:** 그래프의 깊이가 수만~수십만 단위를 넘어가면 자바의 런타임 콜 스택 제한을 초과해 에러가 발생합니다. (이 경우 `Stack` 객체로 명시적 구현을 하거나 BFS를 고려해야 함)

### 핵심 요약도
[DFS 함수 진입]
       │
       ▼
[1. 기저 조건 확인] ──(만족 시)──> [결과 갱신 후 return]
       │ (불만족 시)
       ▼
[2. 다음 분기 순회 (for 루프)]
       │
       ├── 조건 검증 (방문 여부? 범위 내?)
       ├── 상태 전이 (방문 처리 등)
       ├── dfs(다음 노드, 누적값) 호출
       └── 상태 복구 (필요시 백트래킹)

### 실전 선택 기준 (DFS vs BFS)

* **DFS를 선택해야 할 때:**
* 모든 경우의 수를 하나씩 확인해야 할 때 (완전탐색, 백트래킹)
* 탐색 과정에서 지나온 경로의 제약이나 특징(누적 비용 등)을 파라미터로 계속 들고 다녀야 할 때


* **절대 DFS를 쓰면 안 되고 BFS를 써야 할 때:**
* "최단 거리", "최소 이동 횟수"를 묻는 문제 (미로 탈출 등)


---
     */
}
